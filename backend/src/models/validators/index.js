const validateHour = require('./validateHour');
const validateEmail = require('./validateEmail');
const validateStringLength = require('./validateStringLength');

module.exports = {
  validateHour,
  validateEmail,
  validateStringLength,
};
